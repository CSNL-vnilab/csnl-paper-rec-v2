<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.csnl.paper-rec.grm</string>

  <!-- Wednesday (Weekday 3; 0/7=Sun) 15:00 KST: scan the NAS GRM/2026 folders
       and upsert archive_meeting_materials. 15:00 leaves headroom after the
       GRM (~13:00 end) for the slides to land on the share. TZ=Asia/Seoul
       forces KST. The wrapper guards once-per-week and only runs on/after
       Wed 15:00 KST. -->
  <key>StartCalendarInterval</key>
  <array>
    <dict><key>Weekday</key><integer>3</integer><key>Hour</key><integer>15</integer><key>Minute</key><integer>0</integer></dict>
  </array>

  <key>WorkingDirectory</key>
  <string>/Users/csnl/Documents/claude/csnl-paper-rec</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>/Users/csnl/Documents/claude/csnl-paper-rec/scripts/weekly/run_grm_ingest.sh</string>
  </array>

  <key>EnvironmentVariables</key>
  <dict>
    <key>TZ</key>
    <string>Asia/Seoul</string>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
  </dict>

  <key>StandardOutPath</key>
  <string>/Users/csnl/Documents/claude/csnl-paper-rec/state/cron-grm.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/csnl/Documents/claude/csnl-paper-rec/state/cron-grm.log</string>

  <!-- RunAtLoad=true so a Mac that was powered off at Wed 15:00 KST runs the
       missed scan on next boot. The wrapper's once-per-week guard
       (state/grm_last_run_week) makes load/boot fires idempotent and only runs
       on/after Wed 15:00 KST. -->
  <key>RunAtLoad</key>
  <true/>
</dict>
</plist>
