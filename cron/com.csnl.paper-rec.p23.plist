<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.csnl.paper-rec.p23</string>

  <!-- Wednesday (Weekday 3; 0/7=Sun): 08:00 KST = PaperBlitz present-day email;
       14:00 KST = the weekly routine (capture → refill → send → mirror) + the
       new-recs email. TZ=Asia/Seoul forces KST. The wrapper routes by hour and
       guards once-per-week / once-per-day. launchd re-fires a missed slot on wake. -->
  <key>StartCalendarInterval</key>
  <array>
    <dict><key>Weekday</key><integer>3</integer><key>Hour</key><integer>8</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Weekday</key><integer>3</integer><key>Hour</key><integer>14</integer><key>Minute</key><integer>0</integer></dict>
  </array>

  <key>WorkingDirectory</key>
  <string>/Users/csnl/Documents/claude/csnl-paper-rec</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>/Users/csnl/Documents/claude/csnl-paper-rec/scripts/weekly/run_weekly_cron.sh</string>
  </array>

  <key>EnvironmentVariables</key>
  <dict>
    <key>TZ</key>
    <string>Asia/Seoul</string>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
  </dict>

  <key>StandardOutPath</key>
  <string>/Users/csnl/Documents/claude/csnl-paper-rec/state/cron-p23.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/csnl/Documents/claude/csnl-paper-rec/state/cron-p23.log</string>

  <!-- RunAtLoad=true so a Mac that was powered off at Wed 14:00 KST runs the
       missed routine on next boot. The wrapper's once-per-week guard
       (state/p23_last_run_week) makes load/boot fires idempotent and only runs
       on/after Wed 14:00 KST. -->
  <key>RunAtLoad</key>
  <true/>
</dict>
</plist>
