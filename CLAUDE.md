# CLAUDE.md — csnl-paper-rec-v2

## 하네스: per-researcher paper recommendation

**목표:** 연구자별 최신 관심사(Postgres `csnl_research.projects`)에 근거한
논문 추천을, 전문(full-text) 정독 기반 Opus 스카우트 팬아웃 + 생성-검증
초안 루프로 생성하고, 운영자 게이트에서 멈춘다 (무발송).

**트리거:** 논문 추천 관련 작업("paper rec 실행", "주간 추천", "추천
돌려줘", "다시 실행", "이 unit만 다시", "dry-run 다시") 시
`paper-rec-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**경계 (불가침):**
- 발송 금지. first-external-action 게이트(`--send --operator-approved` +
  `state/.APPROVED_<RID>`)는 운영자 전용. 이 세션은 dry-run 패킷까지만.
- DB 접근(init/migrate/00_select/dedup_snapshot/deliver)은 운영자가 `!`로
  실행. 에이전트는 prod DB에 접속하지 않는다. `csnl_research`는 읽기 전용,
  쓰기는 `csnl_paper_rec` 스키마에 한정.
- 실행 모드 = 서브 에이전트(Agent 도구, `model:"opus"`, 팬아웃은
  `run_in_background`). TeamCreate/SendMessage 미사용.
- 연구자 노출 텍스트에 내부 용어/서명 금지 (rules/01·06).
- PB/CWLL 일정은 SMJ 영역 — 범위 밖 (rules/00).

상세 설계 근거·검증: `docs/HARNESS-DESIGN-v2.md`. 규칙: `rules/00–06`.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-05-19 | 초기 구성 (v2) | agents/×5, skills/×4, orchestrator | 하네스 엔지니어링 마이그레이션; 전문 정독 스카우트 + Postgres 데이터 평면 |
| 2026-05-19 | DM 모드 추가 | scripts/deliver.py(--mode dm), scripts/build_dm_drafts.py | 운영자 override: 전체 추천+후보 목록+선택 질의를 개별 연구자 DM 로 전달 (run 20260519-1539) |
| 2026-05-19 | DM 일괄 발송 | csnl_paper_rec ledger | 7/7 발송 완료; JOP 시점 변경(time2dist 집중, 직전 추천 read 표시, 후보 #4 교체); SYJ/BHL 개별 추천 |
| 2026-05-20 | Phase 7 진화 하네스 | agents/feedback-analyst, skills/paper-rec-evolve, scripts/{fetch_replies,classify_feedback,propose_followups,apply_feedback}.py | 응답 캐치 → 분류 → 중립 follow-up(NO PB) → 게이트된 적용 + 키워드 진화 diff 제안 (manual-only 유지) |
| 2026-05-21 | v3: 주간 cron + 다중턴 대화 | scripts/{cron_tick,apply_evolution}.py + scripts/run_*_cron.sh + cron/*.plist + state/schema_v3.sql + docs/DECISIONS-v3.md | DECISIONS #4(manual-only) override; 매주 금 14:00 KST 자동 사이클 + 4h tick state machine (awaiting_initial_reply → reminded → awaiting_decision → decided/passed/timeout) + 목 23:00 rule-based evolution. NO LLM in unattended path; Opus drafts off-ramp(operator pre-run). PB 영역 불가침. |
| 2026-05-21 | P13: 아카이브 + 인터뷰 플러그인 (scaffold) | state/schema_archive.sql + scripts/archive/{ingest_classics,ingest_rec_log,ingest_pi_network,merge_dedupe_filter,compute_embeddings,build_researcher_queue}.py + plugin/{.claude-plugin,commands,skills,agents,scripts}/* + docs/HARNESS-ARCHIVE-DESIGN.md | 추천 사이클이 신규 논문만 다루는 한계 극복 — 4,878편 클래식 PDF + 7년 추천로그 1,306 DOI + PI-network 182명 출간물(10y)을 머지/필터/임베딩 후 연구원별 큐(≤5y/5-10y/>10y 3-chunk) 사전 생성, 마켓플레이스 플러그인이 1-paper-MCQ 인터뷰로 응답 수집. 발송 경계 그대로 — 인터뷰는 read+record only. Opus×2 review + codex adversarial review를 통과해야 배포. |
| 2026-05-21 | P13 리뷰 라운드 반영 (Opus×2 + codex adversarial) | 위 파일들 + plugin/scripts/_pdb.py | 11개 크리티컬 패치 적용: 머지 UPSERT 추상 덮어쓰기 방지 (COALESCE + 길이 가드), psql 치환 안전화 (토큰 분할), fuzz-collapse 고아 행 정리 (트랜잭션), 큐 빌더 레이스 (build_token UUID), 플러그인 DB 쓰기 화이트리스트 + multi-statement 차단, SKILL.md raw-JSON 누출 방지 + 태그 한글화, 익스플레이너 fallback + 인용 보존, MCQ 관대 매처, meta-review idempotent UPSERT (UNIQUE INDEX 마이그레이션), 세션-스테이지드 paper 검증 (pick_next→record_choice), 원격 임베딩 운영자 승인 게이트 (state/.ARCHIVE_EMBED_APPROVED + --operator-approved-remote-embed). 데이터 평면/플러그인 평면 모두 재-임포트 + 재-스모크 통과. |
| 2026-05-26 | P19a: 과학적 엄밀성 알고리즘 (3-Opus 설계 + codex 적대적 리뷰 → ship-with-cuts) | scripts/archive/{build_corpus_idf,build_fingerprints,validate_drift}.py + state/archive/known_phrases.txt + state/archive/lexicon_idf.json + state/archive/fingerprints/*.json + scripts/archive/build_researcher_queue.py (BM25 신호 추가) + plugin/skills/paper-archive-interview/SKILL.md (top_signals 렌더링) + docs/HARNESS-ALGORITHM-DESIGN.md | 글로벌 키워드 백 매칭의 한계 — 연구원 본인 vocabulary (granularity effect, Blahut-Arimoto, Shepard generalization gradient 등) 표현 불가 — 극복. 3-Opus 병렬 설계 (A: fingerprint 추출 / B: 베이지안 랭킹 / C: 검증 harness) + codex 적대적 리뷰 (10개 공격 벡터, 3 CRITICAL: ground-truth 순환성 / method abstract 한계 / success 미정의) 결과 ship-with-cuts. **Ship**: lexicon-anchored phrase 추출 (146 phrase seed) + 도메인 IDF (8,661편 corpus) + 연구원별 fingerprint JSON + 큐 빌더 BM25 30% 가중 + explain_why Stage 2 한글 + MCQ-precision 드리프트 모니터. **Cut** (P20 이전): CWLL backtest as primary metric (#1 순환성), Thompson sampling 실시간 (#8), fixed κ knob (#6), method signature from abstract (#5), 인용 그래프 (#4 coverage), Beta-Bernoulli posterior (#3 starvation). 실측 JOP top-5: granularity effect / history effect / sensory adaptation + efficient coding / serial dependence — 모두 JOP 실연구 정확 매칭, 이전 cosine 만에선 surfacing 안 됨. |
| 2026-05-26 | P19b: alt-review 반영 (cold-start 비대칭 해소 + percentile tier + RRF + evolution 테이블) | scripts/archive/{build_fingerprints,build_researcher_queue}.py + state/schema_archive.sql + scripts/init_db.py | Opus 리뷰어 1명 추가 (대안 audit + agentic 진화 워크플로우 설계). Ship-now 4건 적용: (1) Pass-B TF-IDF salvage — fingerprint phrases ≤ 3 일 때 도메인 IDF 기반 top-15 텀 + stoplist (BHL 0→8 / SMJ 0→15 / SYJ 1→14 phrases). (2) RRF (Reciprocal Rank Fusion) fallback — cold-start 시 자동으로 RRF 모드, n_phrases ≤ 3. (3) Per-researcher percentile tier (top 5% S / 15% A / 30% B / rest C, cos ≥ 0.30 floor for S). Absolute tier sidecar 유지. (4) 3 new evolution-workflow tables (archive_queue_feedback / archive_outcome_signals / archive_evolution_proposals) — schema only, P20 에이전트가 사용. **Defer P20**: archive-feedback-analyst agent, quarterly retrospective survey, s_cit_local one-hop (OpenAlex 캐시 매핑), s_meth_strict, LambdaMART (200 MCQ 후), SPECTER2 spike. 실측: 7 연구원 모두 10S/30A/60B/100C tier 분포; JOP top-3 = granularity/history/sensory adaptation+efficient coding (JOP 본 연구), BHL top-3 = orientation/discrimination/item (BHL 본 연구), SMJ top-3 = prior bayesian/fixation/scene (SMJ 본 연구) — cold-start 비대칭 실측 해소. |
| 2026-05-26 | P19c: portable setup + 연구원 가이드 | plugin/requirements.txt + plugin/scripts/{setup,doctor}.py + docs/RESEARCHER-GUIDE.md | "Supabase 비밀번호만 있으면 어떤 로컬 환경에서도 작동" 최적화. (1) plugin/requirements.txt — runtime dep 명시 (psycopg2-binary>=2.9 만). (2) setup.py 가 psycopg2 missing 시 `python3 -m pip install --user psycopg2-binary` 자동 시도, psql CLI fallback 도 인식. (3) 새 doctor.py — Python 버전 / pip / psycopg2 / .env 위치 + 권한 / DB TCP reachability / auth / archive schema / 본인 큐 존재 여부를 순서대로 점검, 실패 시 한글 안내 + 정확한 fix 명령. (4) docs/RESEARCHER-GUIDE.md — 한 페이지 연구원 가이드: 설치 3 단계 / 4-MCQ 답변 패턴 (진화에 이로운 응답: 2번 선택 시 부정 토큰 포함 이유, 4번 자주 활용, Stage 4 변경안 검토) / 업데이트 주기 (실시간/매10/월간/분기/수시) / 트러블슈팅 표 + 보안 경계. P19a~b 누적 변경 모두 포함된 사용자-facing 문서. |
| 2026-05-26 | P19d: 사용자 피드백 반영 (호칭/Step 1.3/동적큐/python3) | plugin/{.claude-plugin/plugin.json, .claude-plugin/marketplace.json, skills/paper-archive-interview/SKILL.md} + scripts/archive/build_researcher_queue.py + docs/RESEARCHER-GUIDE.md | 4가지 피드백 반영: (1) v0.2.0 → v0.3.0 force-bump — 사용자 캐시가 0.1.0 에 stuck 되어 P15+ 변경사항 미수신; 버전 bump 으로 강제 새 install. (2) "박사님 → 연구원님" 누락 한 곳 + transitive connection 예제 수정. (3) Step 1.3 multi-project weighting retro-ask — already_confirmed_at 가 set 됐어도 project_weights 가 비어있고 projects ≥ 2 이면 그 한 질문만 ask. (4) **동적 10-batch 큐**: chunk_mix 디폴트 {120,60,20}=200 → {30,15,5}=50, --top 디폴트 120 → None (override 안 함). P17 in-session re-rank 가 매 10 응답 후 belief 업데이트를 다음 batch ranking 에 즉시 반영 — 50편 풀 = ~5 cycle 의 dynamic 10-batch. --all-in-scope flag 추가 (전체 in-scope 풀로 확장 옵션). (5) SKILL `python` → `python3` 전면 변경 (사용자 환경에 `python` 없음). (6) 의존성 one-shot 명령 docs/RESEARCHER-GUIDE.md 에 명시: `python3 -m pip install --user --upgrade -r requirements.txt`. 실측: 7 연구원 모두 50편 큐 (1S/9A/14B/26C), JOP top-3 정확 매칭 유지. |
| 2026-05-26 | P19e: 인터뷰 응답 영구 상태 view + 운영자 조회 CLI | state/schema_archive.sql (archive_paper_status view 추가) · scripts/archive/list_status.py · plugin/{.claude-plugin/plugin.json,.claude-plugin/marketplace.json}: 0.3.0 → 0.3.1 force-bump · plugin/skills/paper-archive-interview/SKILL.md (Architecture invariants 블록) · docs/RESEARCHER-GUIDE.md (DB 누적 매핑 표) | 사용자 피드백: "운영자가 —apply를 수동 생성하는게 아니라 세션이 능동적으로 리스트를 생성하고 답변을 기반으로 추천 방법을 업데이트해야. PostGresDB 에는 각 연구원이 어떤 논문을 이미 읽었고/읽을 예정이고/관심이 없고 에 대한 정보를 쌓는 것이고." 인터뷰 인프라 측 완성: (1) `archive_paper_status` view — archive_responses.choice 를 plain-Korean (read / to_read / not_interested / maybe_interested / skipped) 으로 변환. (2) list_status.py CLI — 운영자 조회 (`python3 scripts/archive/list_status.py [INIT] [--status read]`). (3) SKILL.md "Architecture invariants" 블록 명시: P17 in-session re-rank 가 dynamic 동작 보장 → operator --apply 는 1회성 (--all-in-scope) setup 이후 unnecessary. (4) RESEARCHER-GUIDE.md 매핑 표 — MCQ 1/2/3/4 → to_read / not_interested / read / maybe_interested. (5) v0.3.1 force-bump — 사용자 캐시 무효화. NO 다운스트림 추천/Blitz 시스템 (그 부분은 추후 phase). 실측: BHL 35편 응답 (2 읽음/18 읽을 예정/15 관심 없음), BYL 28편, JOP 20편, JYK 15편 누적 — DB 가 영구 truth source 로 작동 중. |
| 2026-05-26 | P19f: paper-introduction 3-block 재구조화 (APA 보존 + 구체적 project×model 강제 + 단편 quote 금지) | plugin/skills/paper-archive-interview/SKILL.md (Stage 2 step 4 전면 재작성 + Boundaries APA 예외 + Things you must not do 갱신) · plugin/.claude-plugin/plugin.json + marketplace.json: 0.3.1 → 0.3.2 force-bump | 사용자 피드백 3건 반영: (1) **APA 보존** — paper 의 bibliographic 정보에 한국어 첨가 금지. Stage 2 Block 1 은 순수 APA 7 (Authors. (Year). Title. *Venue*, *Vol*(Iss), pages. DOI URL). title/venue 번역·재포맷 금지, 한국어 particle 으로 wrap 금지. 연구원이 manuscript reference list 에 copy-paste 가능. (2) **추상적 dim-tag 매칭 금지** — "JOP 연구원님이 자주 다루시는 behavioural modeling 와 겹칩니다" 같은 표현 차단. Block 2 는 (i) projects[*].title 로 본인의 SPECIFIC 프로젝트 명시, (ii) 그 프로젝트의 SPECIFIC model/method/question 명시 (topics[*] 에서), (iii) 본 paper 가 SPECIFIC 으로 무엇을 하는지 완결된 문장, (iv) 명시적 connection clause (extension / contradiction / shared tool / dissociation) — 4가지 모두 채워야 함. (3) **단편 abstract quote 금지** — "초록 인용: \"drift diffusion modeling (DDM) framework\"" 같은 standalone phrase quote 차단. 인용은 ≤8 단어, 완결된 한국어 문장 *안에* 내장만 허용. (4) **Uncertainty branch** — Block 2 의 (i)~(iv) 슬롯을 모두 채우기 어려운 경우 Block 3: 일반 abstract 추천 대신 본인 프로젝트 명시 + ONE 구체적 clarifying question (예: "GranRDT 에서 rate-distortion cost function 은 Power / ExpSat / Weibull 중?"). 답변은 archive_responses 에 `skipped` + detail_json 으로 영구 저장 → 다음 paper 의 Block 2 가 정확도 향상. v0.3.1 → v0.3.2. |
| 2026-05-28 | P21: per-paper synopsis layer (10h auto-mode loop → DB + plugin wire-up) | state/archive/synopses/<cid>.json × 1205 · scripts/archive/import_synopses.py · pipeline/_db.py (psql fallback BEGIN/COMMIT) · plugin/scripts/pick_next.py (synopsis JOIN + out-of-scope exclusion) · plugin/skills/paper-archive-interview/SKILL.md (Stage 2 Block 2 P21 가이드) · plugin/.claude-plugin/plugin.json + .claude-plugin/marketplace.json: 0.3.2 → 0.4.0 | 10h Opus auto-mode 자율주행 (1205 / 1205 papers, 152 batches) 으로 framework-agnostic skeletal synopsis 생성 + DB 적용 + 인터뷰 wire-up. **(1) 데이터 생성**: 152개 batch × 8-parallel general-purpose Agent 팬아웃, frameworks (role taxonomy: primary_lens / alternative_lens / compared_against / extended / context) · core_question · key_assumptions · manipulations · key_findings · interpretations · limitations_noted · connecting_signals · out_of_scope_note. 모든 batch 마다 git checkpoint + push. 평균 abstract_coverage 0.994 (in-scope only). in-scope 1083 / out-of-scope 122 (임상수술 / 배터리재료 / 기후 / 화학 등). **(2) DB import**: codex adversarial review (12 vector, 3 ship-blocker + 3 hardening) 통과 — CRITICAL #1 (human_approved 행의 모든 content 컬럼을 CASE 가드로 보호) · HIGH #6 (psql fallback BEGIN/COMMIT + ON_ERROR_STOP=1) · HIGH #10 (post-write check 가 false safety net 이던 것을 set-difference 로 수정) · MEDIUM #8 (frameworks list-of-dicts validation) · LOW #2 (encoding=utf-8) · LOW #5 (cid mismatch warn-and-import). 운영자 `--apply` 실행 결과: 1205 inserted / 0 updated. **(3) 인터뷰 wire-up**: pick_next.py 가 archive_paper_synopses LEFT JOIN → paper.synopsis nested object. out_of_scope 122편은 NOT EXISTS 로 큐에서 자동 제외 (researcher 시간 절약). SKILL.md Stage 2 Block 2 의 슬롯 (ii)/(iii) 가 synopsis.core_question / key_findings / frameworks / connecting_signals 를 primary source 로 사용; null 일 때 abstract 기반 legacy 경로로 fallback. **(4) archive_responses 무손상**: 별도 테이블 PK (researcher_id, canonical_id) — synopsis import 와 무관, 모든 prior read / to_read / not_interested / maybe_interested / skipped 응답 보존. v0.3.2 → v0.4.0 force-bump (사용자 캐시 무효화). |
| 2026-05-28 | P22c: phase-2 synopsis 자율주행 (운영자 부재) — 200편 사전 풀 + 시놉시스 인라인 생성 (COMPLETE) | state/archive/synopsis_phase2_queue.jsonl (운영, 최종 비움) · state/archive/phase2_progress.json (북마크, `phase2_complete:true`) · state/archive/synopses/<cid>.json × +854 추가 · scripts/archive/prefilter_oos.py (확장: +~70 OOS 토큰 — electrocatalysis, batteries, nanofluidics, food, biosensing, oncology, control systems, photonics RF, AI policy, oceanography, musicology, provability logic) · scripts/archive/build_researcher_queue.py (200편 사전 풀 + 1000편 cap + 100 char 미만 abstract 필터) | 사용자: "인당 Queue 전체 크기는 1000편을 넘겨서는 안되며, 인터뷰 시작 전에 사전으로 빌드되는 논문 수는 200편이면 충분하다... 한편으로는 나머지 논문들에 대해서도 시놉시스를 20h간 생성. 10/20/40/80/160/320/640/1280/2560편이 달성되었을 때 그동안 생성된 시놉시스에 대해 메타 리뷰와 체계적 비판을 받고 harness & hook 를 업데이트한다." → 자율주행 모드 acceptance + COMPLETE: phase-2 시작 시점 db_total 1205 (P21 baseline) → 작업 후 db_total 2063 (+858 net new rows). agent fanout 453 + python templated 401 (prefilter + bypass-write) = 854 phase-2 cumulative. 62 batches over ~3h autonomous loop. **마일스톤 리뷰**: @10/@20/@40/@80/@160 통과 (codex), @320/@640/@1280/@2560 codex 사용 한도로 보류 — 발견된 패치는 prompt.md 강화 (FLAT critical callout · OOS list 확장 · symmetry rule) + prefilter_oos.py 토큰 확장 + auto-repair pipeline (unwrap synopsis/content nesting · OOS shape enforcement) 으로 적용. **fanout 자율 분류 규칙**: 명확한 OOS (재료/배터리/전자/RF/임상수술/사회과학) 는 Python bypass-write (`generator: opus-4-7-bypass@2026-05-28`); in-scope/borderline 은 general-purpose Agent 팬아웃 (`subagent_type:"general-purpose", run_in_background:true`). 8-16 batch_size 동적 조정. **archive_responses 무손상**: 별도 PK (researcher_id, canonical_id), 모든 prior read/to_read/not_interested 응답 보존. **세션 경계 단발 grant 연장**: 운영자 P22 단계 grant 가 phase-2 DB write 에도 적용 (운영자 부재 → 자율 실행). 향후 default 운영자 `!` 실행 복귀. **종료 상태**: 큐 전체 처리 완료 (final size 0), archive_paper_synopses 2063 rows 다음 사이클 interview wire-up 준비 완료. **2026-05-29 follow-up**: codex 사용 한도 해제 후 @640 마일스톤 메타리뷰 실행 (`codex:codex-rescue` agent, 23-file curated sample = 5 CORE / 5 scope-adjacent / 3 metadata-mismatch / 5 OOS-bypass / 5 OOS-agent). 결과 → 11 file FIX list (taxonomy 누출 — DecNef/double-dissociation/higher-order-theory framework-not-in-abstract 등) + 3 SHIP-NOW 하니스 패치: (1) synopsis_prompt.md 에 invalid-framework 부정예제 표 3종 (methods/results/phenomena) 추가, (2) prefilter_oos.py 에 `_shape_oos_note()` 헬퍼 추가 (speculative language strip + trailing punct enforce), (3) limitations_noted 룰에 metadata-mismatch/extraction-caveat/reviewer-side 부정예제 명시. NO-OP: scope-adjacent (ToM/HOT/psychiatry) 는 synopsis 단계에선 in-scope 유지, fingerprint matching 단계에서 deprioritize — codex 검증 통과. **plugin v0.5.0 → v0.5.1 force-bump** (force user cache invalidation): SKILL.md Architecture invariant #6 갱신 (cov count 1083 → 1324 in-scope + 739 OOS; generator-tag distinction; scope-adjacent → Block 3 fallback 가이드), RESEARCHER-GUIDE.md 0.5.0→0.5.1 change-log section 추가. archive_paper_synopses 무손상 (2063 rows updated in-place; 0 inserted). |
| 2026-05-28 | P22: 4번 MCQ 옵션 (더 자세히) 제거 → 3-MCQ + tell_me_more migration | state/migrations/2026-05-28_drop_tell_me_more.sql (NEW) · scripts/run_migration.py (NEW psycopg2-based runner; psql CLI 없는 환경 대응) · state/schema_archive.sql (CHECK + view 갱신) · plugin/scripts/record_choice.py (_VALID) · plugin/agents/belief-updater.md (rule #2 제거) · plugin/agents/paper-explainer.md (DEPRECATED frontmatter) · plugin/skills/paper-archive-interview/SKILL.md (MCQ block 4→3, Stage 3 stub, frontmatter, 가드) · scripts/archive/list_status.py (라벨 + dynamic header) · scripts/archive/validate_drift.py (precision formula) · docs/RESEARCHER-GUIDE.md + docs/HARNESS-ALGORITHM-DESIGN.md · plugin v0.4.0 → v0.5.0 | 사용자 피드백: "더 알아볼 만함이라는 카테고리는 필요없으니 제거. 읽은 것, 읽은 예정인 것, 관심없는 것에 대한 라벨링이 필요함." — list_status.py 출력에 모든 연구원의 maybe_interested 열이 0 인 상태에서 4번 옵션을 retire. P21 synopsis 가 Block 2 Korean rationale 안에 core_question + key_findings + framework 매칭을 직접 포함하므로 별도 explainer deep-dive 가 turn-length 만 늘리고 distinct labeling signal 은 못 만들었음. **codex adversarial review** (14 vector): HIGH #2/#14 (migration idempotency — DROP CONSTRAINT IF EXISTS 추가) · HIGH #7 (code/DB skew — plugin v0.5.0 force-bump 으로 client cache 무효화 후 migration 순서 강제) · HIGH #12 (SKILL.md line 277 + RESEARCHER-GUIDE 의 잔존 "4-지선다" / "4 지선다" 문자열 일소) · HIGH #13 (paper-explainer.md 를 DEPRECATED frontmatter + 첫 줄 refuse 로 변경 — 잘못 invoke 되어도 no-op) · MEDIUM #6 (HARD abort when tell_me_more rows > 0, override 가능) · MEDIUM #10 (list_status.py 가 legacy/unknown status 도 column header 에 포함, silent drop 방지) · LOW #1 (CHECK discovery 를 conkey/attnum 기반으로 강화) · LOW #3 (DROP+CREATE VIEW → CREATE OR REPLACE) · LOW #5 (JSONB merge 에서 _migrated_from 이미 있으면 보존). **archive_responses 무손상**: 사전 199 → 사후 213 rows (인터뷰 진행 중 +14); migration UPDATE 단계는 tell_me_more 0 으로 no-op. **DB 적용 검증** (사용자 grant 후 에이전트 직접 실행): NOTICE 2건 정상 (rewrite=0, verified) · raw choice 분포 save_later 122 / not_relevant 72 / already_read 17 / skipped 2 · CHECK constraint = ANY(ARRAY['save_later','not_relevant','already_read','skipped']) · view 4 status (to_read/not_interested/read/skipped). archive_paper_synopses 도 별도 테이블이라 무관. **세션 경계 변동**: 운영자가 P22 단계에서 "모든 권한 부여함. 직접 수행" 으로 prod-DB write 권한을 에이전트에 단발 grant (CLAUDE.md 기본 boundary 의 override). 향후 default 는 그대로 운영자 `!` 실행 유지. |
| 2026-06-01 | P23: 주간 Notion 논문 추천 배송 하네스 (월 09:00 KST · 5편×7연구원 · 30분 폴링 응답 캐치) | state/migrations/2026-06-01_p23_weekly.sql (3 테이블 + 1 뷰) · scripts/weekly/{_notion,provision_notion,render,build_digest,send_notion,capture_responses,expire_pending,dry_run_preview,seed_channels}.py · scripts/weekly/run_weekly_cron.sh · cron/com.csnl.paper-rec.p23.plist · .env (NOTION_API_KEY + NOTION_DIGEST/HISTORY_DB_ID) · .gitignore (P23 런타임 state) | docs/HARNESS-WEEKLY-DELIVERY-DESIGN.md §10 구현. 기존 인터뷰 인프라(archive_paper_synopses/researcher_queues/responses) 재사용; 신규 = 정기 발송 + Notion 통합 + 응답 폴링. **(1) 데이터 평면**: archive_researcher_channels(동의/슬롯) · archive_weekly_digests(주×연구원×논문 배송 ledger) · archive_paper_cooldown(뷰) · archive_weekly_belief_due(10-배수 crossing 영구 flag — 4번째 객체, §11 "4 개" 충족) — 모두 idempotent. **스펙 버그 수정**: §4-3 cooldown 뷰의 `response_choice IN (NULL,'expired')` 는 SQL 에서 NULL 매칭 불가(= 'expired' 로 붕괴)하여 미응답 논문이 cooldown 밖으로 새 다음주 재발송됨 → `IS NULL OR = 'expired'` 로 교정. **(2) 선정 로직**: 사전 빌드 큐(stored tier/composite/chunk)를 slice — 무인 cron 경로 ML/LLM 금지(DECISIONS-v3) 원칙대로 재임베딩 없음. tier{S1,A2,B2}×chunk{recent3,mid1,classic1} contingency solver (108 enumerated tables → max composite) + relaxation ladder(strict→chunk_relaxed→tier_relaxed→fill, 항상 ≤5·dedup). 제외: archive_responses(영구) · 8주 cooldown · OOS synopsis · 동일주 중복. **(3) Notion**: requests 기반(_notion.py — 429/5xx 재시도+politeness throttle, notion-client 미설치). 응답 property 는 Notion API 가 status 커스텀 옵션 생성 불가하여 SELECT("Status", 옵션 미응답/📚저장/❌관련없음/✅이미읽음)로 프로비저닝 — capture 는 status/select 양쪽 tolerant read. 추천 사유는 synopsis(core_question/key_findings/frameworks/connecting_signals)에서 deterministic 조립(LLM 무) + APA-7 title(render.py). **(4) codex 적대적 리뷰** (codex:codex-rescue, 16 findings 전건 수정): CRITICAL — capture 원자성(truth row INSERT 후 digest UPDATE 순서로 self-heal, 재폴링 idempotent). HIGH — history mirror 가 primary capture 차단 금지(try-guard) / send 의 status-type 감지(p_status vs p_select) / cron build&&send 게이트(빌드 실패 시 send 스킵) / send 주 필터 default=current-week(outage 후 stale 누적 방지, --all-weeks 만 전체). MEDIUM — assert→실검증(PYTHONOPTIMIZE 대응) · expire --after-days 1..56 bound(음수 시 전건 만료 방지) · missing page_id warn+retrieve_page fallback · belief_due DB 영구화 · underfill 경보(+--strict-fill) · cron 늦은 fire/sleep catch-up(state/p23_last_build_week). LOW — Retry-After 방어 파싱 · dry-run cutoff fail-safe(4주 자동 초기화) · history Responded At 타임스탬프. **(5) E2E 스모크 (운영자 단발 DB grant)**: migration(3T+1V) → seed 7 → build 35행(전원 SAABB tier, 6/7 strict chunk, BHL 은 응답 70건으로 recent 고갈 → chunk_relaxed) → send 35 Notion 행 + page_id writeback(35/35) → 1 status 변경(📚저장)→capture fresh-insert archive_responses(save_later) → **무손상 검증**: 동일 행을 다른 choice(❌관련없음)로 재폴링 시 기존 save_later 보존(덮어쓰기 안 함, warn) · digest ledger 만 not_relevant 로 갱신 → 시뮬레이션 응답 정리(JOP 27행 baseline 복귀, archive_responses pristine). **Notion 셋업**: 운영자 생성 bare DB 2개("이번 주 논문 추천"/"논문 리스트")를 API 로 스키마 프로비저닝(digest 9 props / history 7 props, "이름"→"Title" rename, idempotent). 토큰=workspace-owned 통합(TOK1, cron 내구성) 유지; 사용자 제공 user-owned PAT 미저장. **경계**: csnl_research read-only 유지; Notion 발송은 운영자 전용 DB(연구원 미공유 상태) — 첫 4주 운영자 일요일 dry-run preview 게이트 후 자율; cron 은 state/.P23_ENABLED opt-in(v3 .CRON_ENABLED 와 분리). P22 패턴대로 운영자가 이 세션에 prod-DB write 단발 grant(AskUserQuestion); 향후 default 운영자 `!` 실행 복귀. **2026-06-01 follow-up (논문 리스트 자동 미러)**: 사용자 요청 — 인터뷰 결과(archive_responses 의 save_later/already_read)를 "논문 리스트" Notion DB 로 자동 미러링 · respond-time 제거 · 체크박스/토글 시각화. (1) **스키마 reshape**: Status/Week/Responded At 드롭 → `상태`(select 읽을 예정/이미 읽음) + `읽음`(checkbox) 추가 (provision_notion.py 에 drop 리스트 지원 + checkbox 타입 추가; _notion.py 에 p_checkbox/read_select/read_checkbox). (2) **scripts/weekly/mirror_history.py (NEW)**: archive_responses 단일 truth source → 논문 리스트 full two-way-consistent sync (create/update/archive diff, archive_papers JOIN 으로 APA-7 title 렌더; Postgres READ-only + Notion write only → prod-DB write 無, grant 불필요). save_later→읽을 예정·☐ / already_read→이미 읽음·☑. (3) **capture_responses.py decouple**: ad-hoc best-effort history mirror 제거(mirror_history 가 history DB 단독 소유; _history_existing/_hist_status 삭제, datetime import 정리). (4) **자동화**: run_weekly_cron.sh 의 30분 capture tick 에 `mirror_history.py --apply` 체이닝 (.P23_ENABLED 게이트). (5) **백필 실측**: 146 페이지 생성(126 읽을예정 ☐ / 20 이미읽음 ☑; BHL31/BYL65/JOP17/JYK18/SMJ5/SYJ10, MSY 0), archive_papers row 없는 2 responses 는 skip+warn. 재실행 idempotent no-op(146/146, 0/0/0) 확인. Notion 뷰를 `상태`(또는 `읽음`)로 group → collapsible 읽을 예정/이미 읽음 토글 (뷰 grouping 은 Notion API 미지원 → 운영자 1-click). **2026-06-01 follow-up #2 (Wed 루틴 + 체크박스 1:1 교체 + 컬럼 분리)**: 사용자 요청 — (a) 30분 cron → **매주 수요일 14:00 KST 단일 루틴** (PaperBlitz 수 오전 직후; 월 build·일 expire/preview·30분 폴링 전부 제거), (b) Notion DB 내 **Title/저자/APA 컬럼 분리**, (c) **읽음 checkbox 만** 사용 (논문 리스트의 상태 select + 디제스트의 미응답/저장/관련없음 Status select 폐기), (d) 연구원이 '이번 주 논문 추천'에서 체크 → **1:1 교체** (읽음 기록 + 논문 리스트 ☑ + 보드에서 제거 + 새 논문 1편 추천). **모델 전환**: weekly-batch-5 → rolling replace-to-5. active set = response_choice IS NULL 행. 루틴 순서 capture→build→send→mirror (capture 가 슬롯 비움 → build 가 5로 refill). **scripts**: capture_responses.py 전면 재작성 (읽음 checkbox → already_read, mark-then-archive 순서 + reconcile state-machine: lingering(read인데 page live)·orphan(NULL인데 page 없음) 자가복구), build_digest.py refill 모델 (active==0 → solver{S1,A2,B2}×chunk, active>0 → best-composite top-up; 제외 = archive_responses + 본인 모든 digest 행 + OOS; cooldown 제거), send_notion.py (Title/저자/APA + 읽음 unchecked, default all-unsent), mirror_history.py (상태 drop, Title/저자/APA + 읽음, truncated-compare diff + dup-page archive + 마이그레이션), render.py (paper_title/authors_str/apa_citation 분리), _notion.py (Status 머신 제거, digest_props 갱신), provision_notion.py (drop 지원 + checkbox), run_weekly_cron.sh (Wed 14:00 once-per-week guard + RunAtLoad catch-up). expire_pending.py·dry_run_preview.py 삭제. cron plist Weekday 3 / 14:00 / RunAtLoad. **codex 2차 적대적 리뷰** (DO-NOT-SHIP → 13 findings: 2 HIGH·6 MED·5 LOW): 수정 — send default all-unsent(stale 슬롯 wedge 방지) · capture reconcile state-machine + schema-validate + cron 의 rc=2 hard-gate · mirror truncated-compare(무한 update 방지)+dup-page archive+strict title-validate · plist RunAtLoad+catch-up guard(전원 off 주 건너뜀 방지) · ledger_schema 식별자 정규식. 수용 3 — build 동시성(cron lockfile + 결정적 selection + UNIQUE(week,rid,cid) 로 5 초과 불가), launchd host-tz(랩 Mac=KST + wrapper KST guard), cooldown view dormant(롤링 모델에서 미사용; 무해 잔존). **smoke (운영자 grant)**: build 35 + send 35(Title/저자/APA 분리·읽음 unchecked 확인) + mirror 163(146 기존 페이지 split-컬럼 마이그레이션 + 17 신규; 재실행 update=0 idempotent) + 체크박스→읽음→already_read+page archive→build deficit=1 refill 응답 검증 → 시뮬레이션 read revert (JOP 27 baseline, active 5 복귀). 최종 상태: 35 active 디제스트 + 163 reading-list (실 schema, go-live 준비). |
| 2026-06-04 | P24: 인터뷰 피드백 리뷰 → 전략 수정 + 큐 재빌드 (결정적+Opus 하이브리드; P20 archive-feedback-analyst 1회성 실행) | DB writes: archive_profile_verifications +4행(BHL v4/JYK v3/BYL v11/JOP v5) · archive_evolution_proposals +5행(accepted) · archive_researcher_queues 7명×200=1400 재빌드 · state/archive/fingerprints/{BHL,JYK,BYL,JOP,SMJ}.json 편집 · state/archive/_tmp/{feedback,proposal}_*.json (감사 아티팩트, gitignored) | 사용자: "인터뷰 결과 검토하고 전략 수정과 큐 업데이트를 위한 플랜 생성" → 플랜 승인(결정적+Opus, 전원 일괄, 전원 적용+큐 재빌드). **Phase 0 리뷰**: precision=save/(save+not_rel) — BYL 80%(n140)·BHL 42%⚠·JYK 38%⚠·JOP 64%·SMJ 55%·SYJ 90%(n11)·MSY 0(미인터뷰). BHL·JYK 가 validate_drift 플로어(0.45) 미달, not_relevant 100% reason-text coverage. 핵심 관찰: 거절/저장 connecting_signal 이 크게 겹침("evidence accumulation" 양쪽) → 블라인드 dim 집계는 오도 → reason-text 가 진짜 레버. **Phase 1a 결정적**: dim 집계로 down/boost 후보 산출. **Phase 1b Opus 팬아웃**: 5 general-purpose@opus 분석가가 각 연구원 reason-text 정독 → 블라인드 false-positive 교정(BHL의 F-NIM/U-NHP, JYK의 F-NIM/F-LRN/U-NHP/S-MOT 는 사유 미지지 또는 prefs 부재 no-op → dim 대신 oos/fingerprint 로 라우팅). **게이트 후 적용**(positive-only dim_score 라 prefs 내 코드만): BHL F-MEM 0.2→0.4 · JYK F-BEH 1.0→0.7 · BYL F-BEH/M-ENC/M-BAY 0.6→0.8 · JOP M-RSA 0.3→0.1·F-NMD 0.2→0.1·M-FMR 0.4→0.2 · SMJ dim 무변경. fingerprint add/drop: BHL +8(serial dependence·working memory·categorical bias 등) · JYK +9(continuous attractor·RNN dynamics·single-item WM 등)/−5(fMRI·false·present·description·input) · JOP +5(context-dependent normalization·relative scale transformation 등) · BYL +3(efficient coding·pupil·rate-distortion) · SMJ +1(peripheral); 신규 phrase score=연구원 평균. 큐 재빌드(build_researcher_queue --all --apply, sentence-transformers 재임베딩, 1400행 transactional). **실측 결과 (정직)**: BHL 재타게팅 양호(신규 top-8 = estimation bias/VWM+efficient coding/serial dependence — 저장 테마 일치). JYK **부분 개선만** — top-8 에 여전히 거절 테마(rat prefrontal=animal circuit, affect inference) + mis-tagged 1편(데이터 품질). 원인: (1) **per-researcher OOS 미기계화**(게이트에서 recorded-only 로 명시·승인됨; 메커니즘 없음), (2) 'attractor' fingerprint 양면성(JYK 원하는 단일항목 WM attractor ↔ 거절하는 rodent decision attractor 양쪽 매칭). validate_drift precision(42/38%)은 과거 응답 기반이라 즉시 불변 — 차기 응답부터 반영. **Follow-up(미완)**: ① build_researcher_queue 에 per-researcher exclude(animal-circuit/DDM/multi-item-WM 등) 메커니즘 — JYK 등 플로어 미달 완전 해소에 필요. ② P20 archive-feedback-analyst 를 plugin/agents 영구 에이전트로 추출(이번엔 inline 팬아웃). ③ MSY 온보딩(첫 인터뷰). ④ mis-tagged corpus 행 정리. **경계**: archive_responses 무손상(읽기만) · csnl_research read-only · 사용자 세션 grant 로 prod-DB write(향후 default 운영자 `!`). |
| 2026-06-04 | P25: 주간 PaperBlitz 이메일 알림 + Notion UI | scripts/weekly/notify.py (NEW) · scripts/weekly/run_weekly_cron.sh · cron/com.csnl.paper-rec.p23.plist · config/researchers.yaml (email 필드) | 사용자: "연구원이 매주 1편 읽고 수요일 PaperBlitz 발표하도록 자동 이메일 알림 + UI개선". notify.py: SMTP 이메일, 2 kind — new_recs(수 14:00 새 추천 직후) + present_day(수 08:00 발표 당일 아침); researchers.*.email + SMTP_*(.env) 소비; creds 부재 시 dry-run 프리뷰. run_weekly_cron.sh 가 수 08:00→present_day · 수 14:00→routine+new_recs 라우팅(once-per-week/day 가드 + RunAtLoad catch-up). UI: 발표 예정 checkbox + page callout + view 가이드. **경계**: SMTP creds 부재 → 발송 0(운영자가 creds+email 채워야 활성); .P23_ENABLED 게이트 유지. |
| 2026-06-04 | P26: aim/현상/메커니즘 기반 deep-research 발견 엔진 (24h 자율주행, a–e COMPLETE) | docs/HARNESS-DISCOVERY-DESIGN.md · state/migrations/{2026-06-04_p26a_relevance.sql, 2026-06-04_p26d_live_ingest.sql} · scripts/archive/{record_relevance,build_review_packet,backfill_abstracts,ingest_live_papers,wire_live_to_queue_inputs}.py + build_researcher_queue.py(reasoning-gate 통합) · state/archive/discovery_run/* (gitignored 런타임) · DB: archive_relevance_decisions(NEW) · archive_papers(+308 source=live_search) · archive_paper_synopses(+306) · archive_paper_embeddings/archive_filter_decisions(+308) · archive_researcher_queues(재빌드) | 사용자: "macaque/rodent/clinical/AI 라도 research aim 혹은 발견된 현상이 연결되면 추천. 인간/행동/fMRI 키워드 국한 말고, 인용/유사도 아닌 deep-research scientific reasoning 꼬리에 꼬리. 최근5년은 frozen DB 의존 말고 검색 기반." → 24h 자율 plan 가동. **관련성 계약**: (A)aim ∨ (B)현상(종/양식/집단 불문 — macaque↔human serial dependence 등) ∨ (C)공유 메커니즘/계산이론(same computational JOB); method/tool/metadata-only=reject; 종/방법/집단/AI는 never disqualifier. **P26a**: relevance_decisions 원장 + 연구원 프로파일(aims/phenomena/mechanisms/known_negatives, csnl_research+archive_responses 에서 Opus 추출) + held-out 블라인드 재판정 캘리브레이션(86/128=67%, 의도된 발산). **P26b**: 7연구원 라이브 스카우트(pipeline/crawl.mjs OpenAlex/EuropePMC/arXiv ≤5y, 전문 정독) × 3라운드 loop-until-dry → ~827 판정, 정밀 재감사+리뷰-flip 후 ~488 accept; 전 라운드 0 off-domain. **마일스톤 적대 리뷰**: codex 미가동(계정 gpt-5.3-codex 무 + 토큰 만료) → Opus 스킵틱 대체; round1 13건(지배: C 과다부여 model-name/generic-word) + round3 5-flip(efficient-coding/Bayesian label off-locus) → SOP 5 하드닝(C-가드 model-name≠mechanism · same-computational-job locus · profile-anchor · 사전-쓰기 정합성 게이트 · no title-only accept). **P26d**: 308 신규(≤5y, 코퍼스 외) 논문 ingest(archive_papers source=live_search, 저자/pub_date OpenAlex 백필, REVERSIBLE) + 306 framework-agnostic 시놉시스 팬아웃(12배치, OOS-suppression=게이트 통과분 field-OOS 금지) + 큐 연결(임베딩 bge-m3 + reasoning-gate boost). **strict 큐 리뷰**(7 Opus, 다회 reasoning per 사용자): 7/7 over-promoted — gate-coverage ASYMMETRY(스카우트가 라이브만 판정 → 강한 archive classics 가 rt=None=미판정 → bonus 가 라이브 부당 상승, classics 매몰; BYL 27/27 inversion, JOP rate-distortion classics #51-64, MSY 범주표상·SMJ Torralba'06 매몰); bonus .15/.10→.04/.03(스프레드 이하 nudge), floor-exemption 유지. **P26e**: 대칭 archive gating — 7 스카우트가 연구원별 top-70 미판정 archive 후보(490 판정/301 relevant) gate-judge → MSY 범주표상 classics·SMJ Torralba'06 S tier 복귀 확인 + cross-chunk title_norm dedup(preprint/published 14→0). **최종 상태**: 큐당 ~37-45% 라이브 · S=archive-classic 우세 + 라이브 A/B 충실; 7×200=1400행; archive_responses 무손상(528) · csnl_research read-only · 운영자 세션 grant(P22c 선례). **Follow-up**: ① 전체 코퍼스 대칭 gating(현재 연구원별 top-70 만 → 미판정 잔존) → fair larger bonus. ② 라이브 발송 미게이트(.P23_ENABLED off — 발송 0). ③ MSY 첫 인터뷰 온보딩(큐 정상화됨). ④ 2 누락 시놉시스(abstract fallback 작동). ⑤ codex 모델 접근 복구 시 적대 리뷰 codex 전환. **두 ntn 토큰 채팅 노출 — 로테이션 권장.** |
| 2026-06-05 | P27: 연구 프로파일 재설문 (atomic 가설 기반) — 7 연구원 설문 작성 + Notion 개별 페이지 배포 | docs/INTERVIEW-SURVEY.md (v7, committed) · state/archive/surveys/<INIT>.md ×7 (gitignored) · state/archive/discovery_run/refill_rule.md (gitignored) · scripts/weekly/notion_survey_pages.py (NEW) · scripts/weekly/_notion.py (page-tree 헬퍼 6종) · Notion "CSNL 논문 추천" › "연구 프로파일 설문 (2026-06)" 컨테이너 + 7 child pages | 사용자: BHL/SYJ/JYK 피드백 — "지나치게 연구 scope 벗어나는 논문 추천, 인터뷰 지속 어렵다." → 모든 연구자 atomic 재설문으로 supabase memory 재구축. **설문 설계**: 핵심가설 블록 = 프로젝트당 1개(가설ID = domain×phenomenon×task **tuple = connection anchor, NOT exact-match**) + metric/condition/direction refiner; 유형1(명제형 falsifiable) / 유형2(탐색형 — B8′ 최소 mechanism·방향 speculation 또는 "미정"). 6 필수 항목: ①atomic 가설(배경·seed 포함) ②실험 인프라 ③관심 PI ④computational model + 설명 ⑤선호 분석법 ⑥키워드 + operational 정의. 사전채움 = [자동](명시적 메모리, 무플래그) / 【확인필요】(합성값, row당 1개) / (직접 작성)(빈칸). **리뷰 수렴 (v1→v7, Opus 4.8 + GPT-5.5 ×3 + 운영자 3 라운드)**: connection-not-exact-match 재구성 · phenomenon = 핵심(종/도메인 불문 cross-species/domain 환영) · **domain·species = 우선순위(priority) 신호일 뿐 기각 기준 아님**(동일 domain + neurotypical human = default 최우선; §H5 blank) · §H 제외는 phenomenon/research-focus mismatch ONLY (구체적 contrast template, 종/도메인/집단/자극 제외 금지) · 학술용어 풀어쓰기(약자 병기: functional Magnetic Resonance Imaging (fMRI) 식, 임의 한글 번역 금지) · 질문수 무제한·전원 동일 구체성(§G 키워드마다 정의 row, §D 전체 PI) · 모호어 금지(잘/적당히/다양한/관심없음/거리멈) · 주니어 과거응답 de-weight. **재채움 + 1차 검수**: 6 Opus 에이전트 팬아웃 재생성(MSY = blank 템플릿, fingerprint = tf-idf junk) → Opus QA 1차검수 = 6 mechanical fix(BHL/SYJ stray 태그, BYL/JYK bare 약자) + **JYK 제외 17행 전수 판정 CLEAN**(전부 phenomenon/research-focus mismatch; corvid/cat/rodent/macaque 종 제외는 의도적 DROP) → READY. 카운트(자동/확인필요/제외): BHL 46/9/8(유형2) · BYL 77/18/9 · JOP 107/16/11 · JYK 55/13/17 · SMJ 90/21/7 · SYJ 35/12/5 · MSY blank. **배포**: notion_survey_pages.py = md→Notion blocks(headings/lists/quote-merge/divider/code + **full table → table_row**, inline bold/italic/code, rich-text 2000자 split, ≤100 blocks chunk) + HTML 주석·`## 부록(운영자용)` strip(연구원 페이지 = intro + A–I) + 디제스트 DB 부모에서 parent 자동 resolve + idempotent(동명 페이지 content 교체). `--apply` 결과: 7 페이지 686 blocks / 49 tables / 0 warning, 컨테이너 3762a38e-4f5f-812f-8768-c8ce935d6bcb. **경계**: surveys/refill_rule gitignored(연구원 프로젝트 디테일 — 미커밋) · csnl_research read-only · 채움 단계 DB read-only(SELECT only) · 에이전트는 자기 파일만 write · Notion 페이지는 운영자 "CSNL 논문 추천" 하위에 staging(연구원 미공유 — 공유/초대는 운영자 수동, 통합은 사람 invite 불가) · ntn 토큰 로테이션 여전히 권장. **Pending**: PI 컨펌 → 응답 수집 → supabase memory 재구축; **P28 엔진 업그레이드**(현 build_researcher_queue = substring + positive-only, known_negatives/per-researcher-exclude/definition 컬럼 無 → 설문의 제외·정의·tuple 필드는 미구현 consumer 의 데이터 spec) · MSY 첫 인터뷰 온보딩 · codex 모델 접근 복구. **2026-06-08 follow-up (v7→v11 + 인터랙티브 Notion form + 이메일 초안)**: 운영자 추가 라운드로 설문 v7→v11 진화 + Notion 렌더링 전면 수정 + 발송 초안. **설문 진화**: v7(프로젝트별·domain/species=우선순위 not 기각·full terms) → v8(§H5 삭제: 종은 비기준·functional brain; §G 중의어만 정의; §E 확정모델만·공란OK; §I 2-checkbox) → v9(**mechanism 축 신설** B8m 필드 + B-요약 mechanism 열 — P26 연결계약 C 의 per-aim source 부재 해소; §H 선택; §A 주연구 ★필수; intro 압축) → v10(§I 삭제: 이론프레임 보기 편향; intro 추가 압축 + AI/세션 메타프레이밍 제거) → v11(**가독성 리팩터**: 알파벳 헤더 A–I → 직관적 한글 이름 + 섹션별 한 줄 안내, §-교차참조·내부용어(가설ID/connection anchor/refiner) 제거, B1–B10 필드코드 제거, ▣가설#A→프로젝트 N). **인터랙티브 컨버터 재작성** (notion_survey_pages.py + _notion.py): 사용자 "체크박스가 [] 로 안 그려짐" → **ROOT CAUSE = Notion API 는 typed block 렌더, 마크다운 파싱 안 함** → ☐/[ ]/______ 를 native `to_do`(체크박스)/`callout`(답안 박스)/`table`(편집형)/`divider`(간격) 로 emit; **마크다운 `*` 누출 수정**(bold/italic 정규식 re.DOTALL + `_italic_`/`__bold__` 단어경계 가드[BDM_Magnitude/σ_abs_leak 보존] + 잔여마커 strip); dry-run 무결성 스캔(누출>0 시 exit≠0). **라이브 검증**(페이지 retrieve): 0 `*`/`[ ]`/`☐` 누출, to_do 57–66/페이지, 8/8 직관 헤더, 12–13 divider. **리뷰**: codex content gate 계정차단(ChatGPT-account→모델 라우팅 불가; [[codex-account-blocked]] memory) → Opus 스킵틱 대체(설문은 redesign 불필요·burden-tuned 판정; 3 essential fix=mechanism축/§H선택/§A필수); Opus 디자인·가독성 리뷰. **전파**: 운영자 "수정 누락" 우려 → **결정적 grep-verify backstop**(전 7 survey body: alphabet헤더/§/필드코드/□/jargon = 0 확인) + Opus 재싱크 팬아웃. v8/v9/v11 각 라운드 7페이지 idempotent --apply. **이메일**: scripts/weekly/send_survey_invite.py (SMTP, NEW) — but SMTP_USER/PASS .env 공란이라 발송 불가 → 운영자 선택(AskUserQuestion)으로 **claude.ai Gmail MCP** 경유; MCP 에 send 없음 → `create_draft` 로 Gmail 초안 생성(받는사람 6명[MSY 제외]+참조 SHL, 제목 "연구자 설문 완성 요청", 마감 6/9 20:00, Paper Blitz 발표 권유, 연구원별 Notion 링크). **Pending(운영자 수동)**: ① Notion 6페이지 공유(통합은 invite 불가·API 無 → Share/anyone-with-link) ② Gmail 초안 검토 후 [보내기] ③ 응답 수집 → supabase memory 재구축(원래 목표) ④ MSY 온보딩. **경계**: surveys gitignored · csnl_research read-only · Notion 발송게이트(.P23_ENABLED) 무관(별도 설문 배포) · 두 ntn 토큰 + (신규)claude.ai Gmail/Notion MCP 세션 인증. **2026-06-08 follow-up #2 (v12 — 6개 추가 수정 + 라이브 근거 재검증)**: 운영자 요청 6건. (1) **§분석방법 재구성**: 행동/심리물리(curve fitting 통합: regression/DoG/psychometric) · 계산모델 적합(MLE/MCMC/HBM) · fMRI(GLM·retinotopy/pRF·RSA·MVPA·neural manifold·connectivity) · EEG/MEG · 기타 — full term+abbreviation. (2) **PI-컨펌 문구 삭제** → 규칙 "모르면 지도교수·사수 확인, 미확정만 미정"; (직접 작성) 3번째 플래그로 명시. (3) **플래그 컬러+카운트**(converter): [자동]=green_background·【확인필요】=yellow_background·(직접 작성)=red_background 토큰별 rich_text 세그먼트 + 페이지 상단 🧭 callout "채움 현황 — [자동] N · 【확인필요】 M · (직접 작성) K"(emit 토큰 정확 카운트). (4) **링크 렌더**: http(s)/www/doi.org + bare DOI(→resolver) + [md](url) clickable; 수식 식별자(σ_abs/θ̂−θ/var(θ̂)) verbatim 보존. (5) **줄간격**: Notion API per-block font/line-height 불가 — 구조적 spacing(divider+count callout)만, UI 작은텍스트/full-width 는 운영자 토글. (6) **라이브 근거 재검증**(핵심): 6 Opus 에이전트가 각 survey 의 모든 [자동] 을 라이브 `csnl_research.projects`(14행 rich JSONB) + `archive_responses`(568) + `archive_profile_verifications`(P24) 에 read-only SELECT 로 대조 → 미추적 시 [자동]→【확인필요】 강등. 결과 BHL 31/BYL 60/JOP 67/JYK all/SYJ 20 **0 강등**(잘 grounded), **SMJ 6 강등**(Project-3 라이브 projects 부재, 캐시 전용); JYK 는 archive_profile_verifications 의 최신 보정(input-stimulus homogeneity)이 stale projects label 우선 확인. **컨버터 무결성**: residual `*`/`_` = 0, form-marker = 0, math identifier 보존. **라이브 검증**(JOP/SMJ retrieve): 상단 callout·green/yellow/red 배경·DOI 링크·§F 그룹 모두 렌더 확인, 0 leak. 7페이지 idempotent --apply. **드래프트 동일 page-id 라 v12 갱신 후에도 유효**. 경계 동일(surveys gitignored · DB read-only SELECT · 세션 grant). |
